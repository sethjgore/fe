- wrap_layout :layout do
  .projectsingle__image{:style => "background : url('/projects/#{current_page.data.title}/#{current_page.data.splashimage}');"}
    .row
      .small-12.columns
        %h1.is.centered.hero.display.projectsingle__display 
          %mark
            = current_page.data.title
        %h2.is.centered 
          = current_page.data.summary
  .row
    .small-10.columns
      = yield
  = partial 'layouts/cards'