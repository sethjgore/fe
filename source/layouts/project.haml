= wrap_layout :layout do
  .row
    .small-12.columns
      %h1.block-border
        = current_page.data.statement
    
      %h2 Awaken the activist within you and others through videos & become a stronger leader.

  = partial 'layouts/cards'
  = yield
