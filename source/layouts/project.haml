= wrap_layout :layout do
  .row
    .small-12.columns
      %h1.block-border
        = current_page.data.statement
    
      %h2 Check out some of Facundo Element's recent and ongoing projects. 

  = partial 'layouts/cards'
  = yield
